# Telecom CDR & Billing Insights CLI

This project implements a small CLI for processing telecom CDR batches into a daily billing report.

## Run

```bash
python -m telecom_cli.main --subscribers sample_subscribers.json --cdrs sample_cdrs.csv --output report.json
```
